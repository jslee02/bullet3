FROM ubuntu:bionic

RUN apt-get update -qq
RUN apt-get install -y \
    build-essential  \
    cmake \
    curl \
    git \
    lsb-release \
    pkg-config \
    software-properties-common \
    sudo
